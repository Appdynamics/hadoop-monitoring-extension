//enable compileOnly Dependencies
configurations {
    compileOnly
}

sourceSets {
    main {
        compileClasspath += configurations.compileOnly
    }
    test {
        compileClasspath += configurations.compileOnly
    }
}

idea {
    module {
        scopes.PROVIDED.plus += configurations.compileOnly
    }
}